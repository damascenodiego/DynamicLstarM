(define-sort Feature () Bool)
(declare-const G Feature)
(declare-const A Feature)
(declare-const M Feature)
(declare-const L Feature)
(declare-const C Feature)
(declare-const R Feature)
(declare-const B Feature)
(declare-const N Feature)
(declare-const W Feature)
(declare-const V Feature)
(declare-const Y Feature)
(declare-const P Feature)
(declare-const S Feature)

(assert G)
(assert (and
   (= A G)
   (= M A)
   (= L A)
   (= C G)
   (= R G)
   (= (or B  N  W ) R)
   (not (and B N))
   (not (and B W))
   (not (and N W))
   (= V G)
   (= Y V)
   (= P V)
   (=> S V)
))

(push)
(assert G)
(assert (and 
    (not (and W P))
    (not (and (not W) G))
))
(check-sat)
(pop)
(push)
(assert G)
(assert (and 
    (not (and G G))
))
(check-sat)
(pop)
(push)
(assert G)
(assert (and 
    (not (and (or S (not W)) G))
    (not (and (and W (not S)) G))
))
(check-sat)
(pop)
(push)
(assert G)
(assert (and 
    (not (and N N))
    (not (and W W))
    (not (and B B))
))
(check-sat)
(pop)
(push)
(assert N)
(assert (and 
    (not (and N P))
))
(check-sat)
(pop)
(push)
(assert N)
(assert (and 
    (not (and S P))
    (not (and (not S) N))
))
(check-sat)
(pop)
(push)
(assert N)
(assert (and 
    (not (and N G))
))
(check-sat)
(pop)
(push)
(assert N)
(assert (and 
    (not (and N N))
))
(check-sat)
(pop)
(push)
(assert W)
(assert (and 
    (not (and W P))
))
(check-sat)
(pop)
(push)
(assert W)
(assert (and 
    (not (and S S))
    (not (and (not S) W))
))
(check-sat)
(pop)
(push)
(assert W)
(assert (and 
    (not (and S P))
    (not (and (not S) W))
))
(check-sat)
(pop)
(push)
(assert W)
(assert (and 
    (not (and W W))
))
(check-sat)
(pop)
(push)
(assert B)
(assert (and 
    (not (and B P))
))
(check-sat)
(pop)
(push)
(assert B)
(assert (and 
    (not (and S P))
    (not (and (not S) B))
))
(check-sat)
(pop)
(push)
(assert B)
(assert (and 
    (not (and B B))
))
(check-sat)
(pop)
(push)
(assert B)
(assert (and 
    (not (and B B))
))
(check-sat)
(pop)
(push)
(assert P)
(assert (and 
    (not (and P P))
))
(check-sat)
(pop)
(push)
(assert P)
(assert (and 
    (not (and B S))
    (not (and N S))
    (not (and (or W (not S)) P))
))
(check-sat)
(pop)
(push)
(assert P)
(assert (and 
    (not (and G G))
))
(check-sat)
(pop)
(push)
(assert P)
(assert (and 
    (not (and N N))
    (not (and W W))
    (not (and B B))
))
(check-sat)
(pop)
(push)
(assert S)
(assert (and 
    (not (and W S))
    (not (and (not W) S))
))
(check-sat)
(pop)
(push)
(assert S)
(assert (and 
    (not (and S S))
))
(check-sat)
(pop)
(push)
(assert S)
(assert (and 
    (not (and S S))
))
(check-sat)
(pop)
(push)
(assert S)
(assert (and 
    (not (and N N))
    (not (and W W))
    (not (and B B))
))
(check-sat)
(pop)
